//esto es lo qeu vamos a exportar en el archivo de la ruta
//son los controladores que manejaran las solcitudes que vienen por aprte del usuario
module.exports = {
    productosControl: require('./productos.controles'),
    ventasControl: require('./ventas.controles'),
}